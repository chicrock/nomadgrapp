/// Imports

/// Actions

/// Action Creators

/// API Actions

/// Initial State

const initialState = {
  isLoggedin: false,
};

/// Reducer

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

/// Reducer Functions

/// Exports

/// Default Reducer Export

export default reducer;
